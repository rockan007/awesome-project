import {
    StyleSheet
} from 'react-native'
export const style = StyleSheet.create({
    bigBlue: {
        color: 'blue',
        fontWeight: 'bold',
        fontSize: 30,
    },
    red: {
        color: 'red'
    }
})